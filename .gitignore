# Secrets — jamais commités (.env.example est la seule exception versionnée)
.env
.env.local
.env*.local

# Dépendances & builds
node_modules/
.next/
out/
coverage/
*.tsbuildinfo
next-env.d.ts

# Service worker généré par Serwist au build (source : app/sw.ts)
public/sw.js
public/sw.js.map

# Supabase local
supabase/.temp/
supabase/.branches/

# Divers
.vercel
.DS_Store
*.log

# Skills Claude Code (installés localement, pas dans le dépôt)
.claude/skills/
