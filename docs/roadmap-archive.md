# ROADMAP — Archive des sprints

> Sprints détaillés sortis de `ROADMAP.md` par rotation (règle : `.claude/rules/workflow-sprint.md` — max ~4 blocs détaillés dans la roadmap active). Contenu déplacé par couper-coller, jamais réécrit de mémoire.

### Sprint 16 — Garde anti-dérive du payload de rappel (R7) ✅
**Réconciliation d'abord** (loi anti-hallucination) : la carte « Notifications E2E prod + nettoyage push » était en grande partie **déjà faite ou non réalisable en session**. Nettoyage des endpoints 404/410 → **déjà implémenté** (`supabase/functions/send-reminders/index.ts:150-153`) ; flux send→push→Resend→`sent_at` complet et correct ; test payload R7 (4 tests) et isolation RLS `push_subscriptions` déjà présents (`supabase/tests/reminders.test.ts:246-322`). La preuve E2E prod (déploiement, `cron.job_run_details`, push réel) exige la machine de l'auteur + creds + confirmations → reportée comme **tâche opérationnelle** (Sprint 17). **Re-scope (décision auteur)** vers le seul risque corrigeable ici : `supabase/functions/_shared/payload.ts` est une **copie manuelle** de `lib/notifications/payload.ts` (contrainte zéro-import Deno) — aucun test ne les gardait contre la dérive, alors que la garantie R7 « aucun motif » vit dans les deux. **Livré** : `lib/notifications/payload-parity.test.ts` (nouveau) — compare le corps exécutable (du premier `export` à la fin, fins de ligne normalisées) des deux fichiers ; échoue si l'un dérive de l'autre. **Preuve que le test mord** : drift temporaire injecté dans `_shared/payload.ts` → test **rouge** ; restauré → **vert** (constaté en session). Doc `send-reminders/README.md` synchronisée (note de contingence bundler périmée + état « jamais exécutée » retirés ; pointeur vers le test de parité). Aucune migration BD, aucun changement moteur, aucun changement de payload. Revue indépendante `/code-review` high : **0 finding** ; passe `/simplify` : code déjà propre. Gates mesurés : vitest **200** (+1 ; 129 passés + 71 isolation RLS ignorés faute de Postgres local), tsc 0, biome 0, build OK. Branche : `claude/prompt-executer-sprint-m4t4ug`.

### Sprint 10 — Export iCal + conformité Loi 25 ✅
**Livré** : FR-14 (export `.ics` pour iPhone/Google Calendrier) + conformité Loi 25 de base. **Tokens HMAC stateless** (`ICAL_SECRET` serveur seulement, ajouté à `.env.example` et `securite-secrets.md`) : `signIcalToken` / `verifyIcalToken` (`lib/ical/generate.ts`) — comparaison timing-safe, zéro table requise. **Génération `.ics` pure** (RFC 5545) : travailleur = quarts du moteur + écarts (libellé « Absent » uniquement pour `off`, jamais le motif, R7) ; conjointe = libellés génériques uniquement (« Partenaire en quart / disponible »). DTSTAMP dynamique, `buildVcalendar` sans ligne vide, CRLF. **Route `GET /api/ical/[token]`** : `SUPABASE_SERVICE_ROLE` stateless (pas de GoTrue) ; lookup `memberships` → `profile_id` travailleur → `worker_assignments` + `exceptions` filtrées par `profile_id` (R7 structurel : jamais d'exception d'un autre membre) ; Zod à la frontière BD (team + effect + shift). **Page `/politique`** : accessible sans auth, politique de confidentialité Loi 25 (données collectées, finalités, droits, responsable RVP). **Consentement invitation** : case à cocher + lien `/politique` dans `app/invitation/[code]` ; validation côté serveur dans l'action (contourne le bypass HTML). **Cascade Loi 25** : 2 tests BD — suppression conjointe (auth.users → membership cascade, 0 résidu) ; suppression foyer → 6 tables = 0 résidu. Gates mesurés : vitest **175** (+20 : 18 purs `.ics` + 2 cascade Loi 25), tsc 0, biome 0. Branche : `claude/sprint09-coplanification`.

### Sprint 8 — Mise en ligne + filets ✅
**Livré** : MVP **en ligne**. Supabase Cloud (CA/US-est) : migrations appliquées, types régénérés, secrets Edge posés, `send-reminders` déployée (v4) ; pg_cron planifié (`:05`), opérationnel. Vercel : projet lié, variables d'env posées (sans `GRANDFORD_DEMO`), build de prod vert, domaine de prod. **Dette GoTrue soldée** : lien magique reçu et connexion réussie sur le site réel ; OAuth Google validé. **Push reçu sur l'iPhone réel** (PWA installée), payload sans motif (R7 ✅) ; courriel de repli Resend reçu. **Sentry** : DSN réel branché (`NEXT_PUBLIC_SENTRY_DSN` + `SENTRY_DSN` sur Vercel), événement de test reçu sans courriel ni motif (R7 ✅). **UptimeRobot** : moniteur HTTP(s) sur le site de prod (uptime + réveil du projet gratuit). **Sauvegarde** : secret `SUPABASE_DB_URL` posé, 1er run réel de `backup.yml` → artefact `pg_dump` daté vérifié (NFR-11). Filets locaux déjà livrés (session précédente) : CI 4 gates · Dockerfile + docker-compose · `scripts/verifier-restauration.sh` (round-trip 15 tables, 44 fonctions, policies + GRANT, données cœur) · `docs/mise-en-ligne.md` · scaffolding Sentry R7-strict (`lib/monitoring/scrub.ts`) · correction Edge (`verify_jwt = false`, `_shared/payload.ts`, secret Vault). Gates mesurés : vitest **145** (dont 15 isolation RLS + 12 purs scrubber Sentry), tsc 0, biome 0, build OK. Branche de session : `claude/sprint08-mise-en-ligne`.

### Sprint 7 — Notifications ✅
**Livré** : pipeline FR-10 complet (hors envoi réel). **Génération** : `create_exception_with_motif` (migration `…_sprint07_reminders.sql`) matérialise les rappels dans la même transaction que l'écart — échéances **30/7/1 j** strictement futures (date civile America/Toronto), envoi à 09:00 locale, `profile_id null` = tout le foyer (bidirectionnel), `channel push` ; suppression de l'écart → cascade (0 orphelin constaté). **Couche pure `lib/notifications/`** : `echeances.ts` (miroir TS de la RPC — parité SQL↔TS testée par balayage des bords), `payload.ts` (« écart le DATE », zéro motif structurel + repli `FALLBACK_REMINDER`, R7), `push.ts` (décodage VAPID). **Abonnement** : `ActiverRappels` sur la page foyer (deux rôles) → actions serveur → upsert `push_subscriptions` (RLS appareils strictement personnels) ; réception push + clic dans `app/sw.ts` (payload illisible → notification générique, jamais un rappel perdu en silence). **Envoi** : Edge Function `send-reminders` (service_role : rappels dus → push, repli courriel Resend, `sent_at`, destinataire `profile_id` respecté) — **jamais exécutée** (contrainte Docker/Edge, cf. Sprints 2-6) : déploiement + pg_cron documentés (`supabase/functions/send-reminders/README.md`), validation réelle au Sprint 8. Gates mesurés : vitest **133** (dont 13 BD/RLS rappels + push_subscriptions, 17 purs notifications), tsc 0, biome 0, build OK ; preuves BD observées (J+40 → 3 rappels · J+3 → veille seule · cascade → 0 orphelin · payloads inspectés sans motif). Note revue : si un 2ᵉ chemin d'écriture d'exceptions apparaît (FR-9, FR-15), déplacer la génération vers un trigger sur `exceptions`. Branche de session imposée `claude/prompt-executer-sprint-mktcm9`.

### Sprint 1 — Échafaudage + moteur Pitman ✅
**Livré** : dépôt Next.js (App Router, TS strict + `noUncheckedIndexedAccess`, pnpm, Biome, Tailwind, shadcn/ui init, Serwist PWA — manifest + `sw.js`), scaffold `supabase/` prêt pour migrations (sans tables), et le **moteur Pitman pur** (`lib/engine/`) testé en premier — golden encodant les points réels validés (`docs/analyse/01-decouverte/02-cas-utilisation.md:108`) : ancre 3 juin, 11 juin congé A, 25 déc. A travaille, table complète de juin. Gates verts (vitest 27, tsc 0, biome 0, build OK). Couvre FR-1.

### Sprint 2 — Schéma Postgres + RLS + tests d'isolation ✅
**Livré** : 13 tables du domaine (toutes porteuses de `household_id`) en 2 migrations versionnées ; **RLS activée sur les 13** avec politique « membre du foyer » (helpers `SECURITY DEFINER` anti-récursion) ; **étanchéité du motif structurelle** — `exception_private` arrimé à l'exception parente par FK composites, lisible par le seul travailleur propriétaire (la conjointe obtient 0 ligne) ; types BD générés (`lib/database.types.ts`) ; **tests d'isolation** des 3 scénarios (isolation inter-foyers · motif étanche · révocation immédiate) contre un **vrai Postgres**. Gates mesurés : vitest **35** (27 moteur + 8 isolation), tsc 0, biome 0, build OK. Contrainte d'env : stack Docker Supabase indisponible (CDN d'images bloqué) → Postgres natif + impersonation de rôle (comme PostgREST), `scripts/local-db.sh` ; `supabase start` reste la voie normale ailleurs. Fondation de FR-12.

### Sprint 3 — Auth sans mot de passe + foyer ✅
**Livré** : client Supabase typé (`lib/supabase/` : navigateur, serveur, middleware — `@supabase/ssr`) ; pages connexion (lien magique + OAuth Google/Apple), callback, déconnexion ; onboarding (création foyer) ; page foyer (membres, invitation par lien à usage unique, révocation, quitter) — Server Components purs, chaînes dans `lib/i18n/fr.ts`. Migration `…_sprint03_auth_household.sql` : trigger `handle_new_user` (auth.users → profiles), RPC atomique `create_household_with_membership` (SECURITY INVOKER, sous RLS), table `invitations` (RLS propriétaire seul, expiration 7 j), RPC `redeem_invitation` (SECURITY DEFINER, refus par ERRCODE stables GF001-GF004) ; types régénérés. Gates mesurés : vitest **48** (35 + 13 cycle de vie foyer), tsc 0, biome 0, build OK. Contrainte d'env : GoTrue inexécutable localement (Docker bloqué, cf. Sprint 2) → flux d'auth validés **au niveau BD** (insert `auth.users` = effet GoTrue) ; la frontière GoTrue réelle (lien magique, OAuth) reste **à valider contre un projet Supabase Cloud** (au plus tard Sprint 8). Couvre FR-11, FR-12.

### Sprint 4 — Vue « coup d'œil » ✅
**Livré** : accueil connecté = vue « coup d'œil » (FR-2/FR-3) — pastille **Aujourd'hui** (CONGÉ/JOUR/NUIT/SOMMEIL, lisible < 2 s) + bande semaine + grille mois navigable, **moteur exécuté côté client** (hors-ligne constaté, NFR-4) ; écarts `exceptions` superposés (couche pure `lib/schedule/`, marqueur d'écart propagé au sommeil dérivé) ; sommeil après quart de nuit (`sleep_defaults`, sinon heuristique 8 h documentée) ; **vue conjointe = disponibilité (travaille/disponible/sommeil) sans motif** — payload réseau inspecté, 0 champ motif (R7) ; **sélection d'équipe A/B/C/D** (accueil 1re fois + page foyer, upsert `worker_assignments`) ; au-delà de ±62 j d'écarts chargés, la grille l'annonce (jamais de faux « horaire normal » silencieux). Décision : gabarit = `GRANDFORD_CYCLE` côté client (l'ensemencement `cycle_templates` attendra FR-17). Gates mesurés : vitest **74** (dont 12 isolation RLS, +4 sur les données de la vue), tsc 0, biome 0, build OK ; preuve Playwright sur les points réels validés (11 juin CONGÉ · 25 déc JOUR · écart sans motif · hors-ligne). Contrainte d'env (GoTrue indisponible, cf. Sprint 3) : preuve à l'écran via `/demo/horaire` (activable seulement par `GRANDFORD_DEMO=1`, données factices). Note : branche de session imposée `claude/prompt-executer-sprint-vgmvtn` (environnement distant) au lieu du nom standard.

### Sprint 5 — Capture d'exception ≤ 3 taps ✅
**Livré** : flux de capture complet (FR-4/FR-5/FR-7) — bouton accueil → 6 tuiles → confirmation ; jour par défaut = aujourd'hui ou jour tapé dans la grille ; **OT = 2 taps** (enregistré dès le tap de sa tuile, FR-7), autres tuiles = 3 taps — **comptés à l'écran** (Playwright sur `/demo/horaire?capture=1`, flux démo sans BD : mêmes composants + même sémantique pure, contrainte GoTrue des Sprints 2-4). Sémantique tranchée (`lib/schedule/capture.ts`, pur) : OT → `working_extra` au quart d'identité · congé/maladie/formation/vacances → `off` · échange → `shift_swap` au quart opposé ; motif = la tuile, vers `exception_private` seulement. **Écriture atomique** écart + motif par RPC `create_exception_with_motif` (SECURITY INVOKER sous RLS, doublon de jour → ERRCODE `GF005`) ; **annulation** = suppression par le propriétaire, cascade du motif constatée (0 orphelin). Le travailleur voit son motif (panneau détail) ; la conjointe : payload réseau inspecté, **0 champ motif** (R7). Gates mesurés : vitest **95** (dont 14 mapping tuiles + 7 isolation du nouveau chemin RPC), tsc 0, biome 0, build OK. Note : E2E du chemin réel (actions serveur + revalidation) reste lié à la dette GoTrue → Sprint 8. Branche de session imposée `claude/prompt-executer-sprint-1z0e1d`.

### Sprint 9 — Co-planification conjointe ✅
**Livré** : FR-8 (notes de couple partagées dans le foyer) + FR-9 (requêtes disponibilité : conjointe soumet → travailleur approuve / refuse) + **trigger génération rappels** (prérequis Sprint 7 soldé). **Trigger** : migration `…_sprint09_trigger_reminders.sql` — `generate_reminders_for_exception()` (SECURITY INVOKER, `set search_path = ''`) + `trg_generate_reminders` (AFTER INSERT ON exceptions) ; `create_exception_with_motif` allégée (INSERT reminders retiré) — 0 doublement garanti par test. **RLS requests granulaire** : migration `…_sprint09_notes_requests_rls.sql` — `requests_all` trop permissive remplacée par 4 policies ; UPDATE réservé à `target_profile_id` (travailleur ciblé, conjointe bloquée en test). **Server actions** : `creerNote` / `supprimerNote` (`app/notes/actions.ts`) + `soumettreRequete` / `approuverRequete` / `refuserRequete` (`app/requetes/actions.ts`) — approbation appelle la RPC (`motif = 'requete'` → `exception_private`, R7) puis met à jour `requests.status`. **Composants** : `NoteDuJour` (saisie inline + liste + suppression) ; `PanneauJourConjointe` (modal conjointe : notes + formulaire demande + `StatusBadge` pending/approved/declined) ; `PanneauCapture` étendu (section notes + section requête pendante pour le travailleur). **Câblage** `app/page.tsx` : notes + requêtes chargés en parallèle (±62 j), `parseNoteRows` / `parseRequeteRows` aux frontières Zod ; `requests.body` jamais dans Sentry ni logs (R7). Gates mesurés : vitest **155** (+10 : 7 isolation RLS sprint 9 + 3 trigger), tsc 0, biome 0, build OK. Migrations appliquées sur Supabase Cloud. Branche : `claude/sprint09-coplanification`.

### Sprint 6 — Fenêtre de sommeil par défaut ✅
**Livré** : FR-6 complet. **Configuration unique** : formulaire « Fenêtre de sommeil » sur la page foyer (travailleur seulement), upsert `sleep_defaults`, valeurs proposées = fenêtre configurée sinon heuristique du gabarit. **Auto-application** : déjà câblée dans `dayStatuses` (vérifiée, pas réécrite). **Ajustement au cas par cas** (prémisse 3 de la carte tranchée : table dédiée) : nouvelle table `sleep_adjustments` — unique (foyer, travailleur, date), RLS « membre du foyer », migration `…_sprint06_sleep_adjustments.sql`, types régénérés — exposée dans le panneau du jour tapé quand c'est une journée de sommeil (bloc SOUS la capture : budget ≤ 3 taps des écarts intact, NFR-1) ; retirer l'ajustement ramène le jour au défaut. Priorité dans la couche pure : ajustement du jour > fenêtre configurée > heuristique 8 h ; un ajustement sur un jour sans sommeil est ignoré. La conjointe voit la fenêtre (disponibilité partagée), payload toujours sans motif (R7). Gates mesurés : vitest **103** (dont 15 isolation RLS — 3 nouveaux sur `sleep_adjustments`), tsc 0, biome 0, build OK ; preuve Playwright **11/11** sur `/demo/horaire` (fenêtre configurée affichée le jour post-nuit · repli heuristique 07:00-15:00 sans config · ajuster UN jour ne change pas les autres · retrait = retour au défaut · vue conjointe sans motif) — contrainte GoTrue inchangée (cf. Sprints 2-5). Branche de session imposée `claude/prompt-executer-sprint-kk603d`.

### Sprint 11 — Nom du travailleur page foyer ✅
**Livré** : section « Travailleur » dans `/foyer` — le travailleur voit et peut éditer son nom (`profiles.full_name`) ; la conjointe voit le nom en lecture seule. **Server action `mettreAJourNom`** (`app/foyer/actions.ts`) : valide via Zod (`max 120`), `UPDATE profiles SET full_name` sous RLS authenticated, revalide `/foyer` et `/`. **Page foyer** : extraction `monNom` et `nomTravailleur` depuis la requête `membres` existante (0 requête supplémentaire) ; section i18n-ready (`nomPlaceholder`, `enregistrerNom`, `erreurNom` dans `lib/i18n/fr.ts`). Gates mesurés : vitest **175** (inchangé), tsc 0, biome 0. Branche : `claude/sprint10-ical-loi25`.

### Sprint 12 — Journal des changements (FR-13) ✅
**Livré** : FR-13 complet — traçabilité partagée dans le couple. Migration `20260619120000_sprint12_audit_trigger.sql` : trigger `trg_audit_exception` (AFTER INSERT OR UPDATE OR DELETE ON exceptions, SECURITY INVOKER, `set search_path = ''`) → `log_exception_change()` — insère dans `audit_log` avec `action` ∈ {`exception_created`, `exception_updated`, `exception_deleted`}, `metadata = jsonb_build_object('on_date', on_date, 'effect', effect, 'shift', shift)` — **jamais le motif** (R7) ; `actor_id = auth.uid()` avec repli `NULL` ; garde FK : `IF EXISTS (households)` avant INSERT. Policy `audit_log_insert` resserrée : `actor_id = auth.uid() OR actor_id IS NULL`. **Couche TS** : `parseAuditRows()` (`lib/schedule/db-rows.ts`, schéma Zod + `safeParse`) + `FORMAT_DATE_COURTE` (`lib/schedule/format.ts`). **Vue foyer** : 6ᵉ requête en `Promise.all` (50 entrées, `entity = 'exception'`) + section « Historique des écarts ». **i18n** : `fr.foyer.historique` (3 libellés d'action). Gates mesurés : vitest **179** (+4), tsc 0, biome 0, build OK. Branche : `claude/sprint12-journal`.

### Sprint 13 — Gabarits multi-usines fondation BD (FR-17) ✅
**Livré** : fondation BD + câblage serveur de `cycle_templates`. Migration seed `20260619212431_sprint13_cycle_templates_seed.sql` : `INSERT … SELECT` idempotent — 5 foyers prod ensemencés. **Helper** `lib/schedule/cycle-template.ts` : `fetchCycleTemplate` + `fetchCycleTemplateWithFallback` (repli `GRANDFORD_CYCLE`, NFR-4). **Câblage `app/page.tsx`** : 6ᵉ entrée `Promise.all` → `template` au moteur. **Tests** : 5 purs + 4 RLS isolation `cycle_templates`. Gates mesurés : vitest **184** (+9), tsc 0, biome 0, build OK. Branche : `claude/sprint13-cycle-templates`.

### Sprint 14 — Sélecteur de gabarit UI (FR-17 fin) ✅
**Livré** : FR-17 complété — sélecteur de gabarit dans `/foyer` + server action sécurisée. **Migration RLS affinée** (`20260619230000_sprint14_rls_cycle_templates_owner.sql`) : `cycle_templates_all` (tous membres) → `cycle_templates_select` (membres : SELECT) + `cycle_templates_update` (propriétaire seul : `is_household_owner` using + with check). **`lib/schedule/predefined-templates.ts`** (nouveau) : 2 gabarits Pitman 2-2-3 (ancres 2026-06-03 et 2026-06-10), `gabaritNomSchema` Zod, `trouverGabarit`. **`fetchActiveGabarit`** dans `lib/schedule/cycle-template.ts` : retourne `{ name, template }` (nom requis pour l'affichage). **Server action `changerGabarit`** (`app/foyer/actions.ts`) : `getUser()` → redirect `/connexion` si session absente + validation Zod + `trouverGabarit` + `UPDATE cycle_templates WHERE is_active = true` sous RLS ; revalide `/foyer` et `/`. **Page `/foyer`** : `fetchActiveGabarit` en 7ᵉ entrée du `Promise.all` ; section sélecteur au propriétaire (`role = 'worker'`) ; `fr.foyer.gabarit.*` i18n. **Revue** : 2 bugs corrigés (missing `getUser()` + fallback `"Pitman 2-2-3"` → `GABARITS_PREDEFINIS[0].name`) ; `find` inline → `trouverGabarit`. Gates mesurés : vitest **199** (+15), tsc 0, biome 0, build OK. Branche : `claude/sprint14-selecteur-gabarit`.

### Sprint 15 — Accessibilité & cibles tactiles ✅
**Livré** : passe UX/UI transverse (audit via skill `ui-ux-pro-max` — choix produit préservés : mode sombre, emojis-pictogrammes NFR-12, budget ≤ 3 taps, payload conjointe sans motif R7). **Lot A — socle** : tokens de couleur sémantiques d'état dans `tailwind.config.ts` (`quart-jour`/`quart-nuit`/`conge`/`sommeil`/`travaille` + `-fg`, dérivés de la palette, teintes inchangées) consommés par les `Record` d'affichage de `vue-coup-doeil.tsx` ; **focus clavier visible global** dans `app/globals.css` (`:focus-visible` outline emerald sur tout élément interactif — une règle couvre boutons/liens/champs) + garde `prefers-reduced-motion` ; nouveau composant partagé `components/ui/bouton-soumettre.tsx` (`useFormStatus` → pending). **Lot B — tactile** : cibles ≥ 44 px (`min-h-11`) sur cases du mois (`h-10`→`h-11`), flèches de nav, boutons « fermer », et liens-texte minuscules (déconnexion, annuler invitation, supprimer note, retirer sommeil, monFoyer). **Lot C — feedback** : `BoutonSoumettre` sur tous les formulaires Server Action (`/foyer`, `/connexion`, `/onboarding`, sommeil) — désactive + « Envoi… » pendant la soumission (anti double-soumission) ; `selecteur-equipe.tsx` passé client avec `TuileEquipe`/`useFormStatus` (tuiles conservées). **Lot D — parité daltonien** : pictogramme d'état ajouté dans chaque case du mois (`ContenuCase`) pour distinguer les statuts sans la couleur ; `aria-hidden` sur les glyphes décoratifs inline. **Lot E — hiérarchie** : `/foyer` regroupé en cartes thématiques (`Carte`, 2 niveaux de titres) — membres · Travailleur · Réglages de l'horaire · Rappels et export · Invitations · Historique ; nouvelles chaînes `fr.commun.envoiEnCours` + `fr.foyer.groupes.*`. **Revue indépendante** (`/code-review` high, 2 angles) : 0 finding ; passe `/simplify` (3 props mortes retirées de `BoutonSoumettre`). Aucune migration BD, aucun changement moteur. Gates mesurés : vitest **199** (inchangé — changements présentationnels sans logique testable), tsc 0, biome 0, build OK. Note env : `biome check .` localement bruité par CRLF (`core.autocrlf` Windows) sur des fichiers non touchés dont l'index est en LF — neutralisé, diff inchangé. Branche : `claude/sprint15-ux-accessibilite`.

**Patch 0.15.1 — contraste congé/disponible** (suite feedback UX externe) : l'état congé/disponible affichait l'emoji `✅` (vert-et-blanc) sur `bg-conge` (emerald-500) — coche « invisible » sur fond de même teinte. Remplacé par `✔` (U+2714 **présentation texte**, sans sélecteur de variation : l'emoji `✔️` se rend vert sous Segoe/Windows) dans les deux `Record` d'affichage de `vue-coup-doeil.tsx` (propage à pastille/semaine/cases/légende) + bouton « Approuver » de `panneau-capture.tsx`. Le glyphe hérite de `text-conge-fg` (emerald-950) → coche sombre contrastée, garantie multiplateforme, parité daltonien préservée (NFR-12). Purement présentationnel : aucune migration, aucun moteur, aucun test impacté. Gates mesurés : vitest **199** (inchangé), tsc 0, biome 0 (fichiers touchés), build OK. Revue `/code-review` high : 0 finding ; vérification octet par octet des glyphes (U+2714 nu confirmé). Branche : `claude/sprint16-ux-contraste`.

### Sprint 17 — Jour de paye (worker-private, déterministe) ✅
**Feature dirigée par l'auteur, hors FR-1→17** (consignée : ce n'est pas dans `docs/analyse/` et `CLAUDE.md` exclut « tracker de paie/OT$/banques »). Cadrage retenu : **marqueur de DATE de paye** (quand la paye tombe), jamais un suivi de montants — visible du **seul travailleur**. **Modèle déterministe** (comme le moteur Pitman, « jamais un CRUD de tous les jours ») : on persiste **une config** (ancre + fréquence), les jours de paye se calculent à la volée. **Moteur pur** `lib/schedule/payday.ts` : `estJourDePaye(date, reglage)` réutilise `civilToDays`/`parseCivilDate`/`mathMod` — `mathMod` gère les dates AVANT l'ancre ; fréquences `hebdomadaire` (7 j) / `aux_2_semaines` (14 j) ; `frequencePayeSchema` Zod. **BD** : table `payday_settings` (PK `household_id,profile_id`) — migration `20260624120000_sprint17_payday_settings.sql`. **RLS étanche (R7)** calquée sur `exception_private_owner_only` : `using (profile_id = auth.uid())` (la conjointe, même membre, lit **0 ligne**) ; `with check (profile_id = auth.uid() AND is_household_member(household_id))` — durcissement issu de la revue : empêche d'écrire une paye vers un foyer dont on n'est pas membre (intégrité multi-tenant). **Câblage worker-only** : `app/page.tsx` ne fetch `payday_settings` que dans la branche `role === "worker"` → `reglagePaye` passé à `VueCoupDoeil` (null pour la conjointe, absent par construction). **UI** : marqueur 💰 (`MarqueurPaie`, aria-label — jamais la couleur seule, NFR-12) sur les cases du mois + badge « Paye aujourd'hui » dans le coup d'œil ; formulaire de réglage (ancre + fréquence) dans `/foyer`, server action `definirReglagePaye` (Zod + `getUser()` + upsert sous RLS). **Types** : entrée `payday_settings` ajoutée à `lib/database.types.ts` (insertion fidèle au générateur — régénération en masse impossible : le Postgres local n'a pas pg_graphql, ce qui aurait supprimé `__InternalSupabase` + `graphql_public` présents en prod ; à régénérer par l'auteur sur Cloud). **Preuve d'acceptation observable** : `/demo/horaire?paye=2026-06-04@aux_2_semaines` — vue **travailleur** = 2 marqueurs 💰 (4 et 18 juin) + badge « Paye aujourd'hui » ; vue **conjointe**, paramètres identiques = **0 marqueur, 0 badge** (non-fuite R7 constatée au rendu). **Revue indépendante** `/code-review` high (4 angles, contexte frais) : 0 bug de correction ; 1 durcissement RLS appliqué (clause membre dans `with check`) + 1 test ajouté ; reste = patterns locaux intentionnels. Gates mesurés : vitest **219** (+19 : 13 moteur paye + 6 isolation RLS, **exécutés contre un vrai Postgres** via `scripts/local-db.sh`), tsc 0, biome 0, build OK. Branche : `claude/prompt-executer-sprint-ci2ohp`.

### Sprint 18 — Garde de parité du miroir de format de date ✅
**Dette technique tirée des SPRINTS SUGGÉRÉS** (carte réconciliée avec le code réel : toutes références vérifiées en session, prémisse valide). La date civile `'AAAA-MM-JJ'` est formatée par **deux** `Intl.DateTimeFormat` strictement identiques mais dupliqués — `FORMAT_JOUR_LONG` (`lib/schedule/format.ts:14`, affichage app) et `FORMAT_JOUR` (`lib/notifications/payload.ts:26`, texte des rappels FR-10) — la duplication étant imposée par la contrainte **zéro-import** de `payload.ts` (consommé par l'Edge Function Deno, hors alias `@/`). Chaque fichier portait le commentaire réciproque « MIROIR ASSUMÉ », mais **rien ne gardait** que les deux produisent la même chaîne : modifier un formateur sans l'autre faisait diverger en silence la date affichée de celle écrite dans la notification (même classe de risque que la dérive du corps du payload corrigée au Sprint 16). **Livré** : `lib/notifications/format-parity.test.ts` (nouveau, pur, aucun mock) — `FORMAT_JOUR` étant privé, sa surface observable est `reminderPayload(date, "day").body` ; le test asserte l'**égalité exacte** `body === \`Écart à l'horaire ${FORMAT_JOUR_LONG.format(dateUTC(date))} (demain).\`` sur 5 dates couvrant les axes du format (bornes d'année 01-01/12-31, avant l'ancre Pitman, 29 février bissextile, date ordinaire). **Revue indépendante** `/code-review` high (2 angles, contexte frais) : 1 finding confirmé — l'assertion initiale en `toContain` était **uni-directionnelle** (une dérive qui *élargit* `FORMAT_JOUR`, p. ex. ajout de `year`, laissait la chaîne plus courte de l'app incluse → faux négatif) ; corrigé en égalité exacte → garde **bidirectionnelle**. **Preuve que le test mord** (constatée en session) : drift `month:"short"` → rouge ; drift *additif* `year:"numeric"` → rouge (capté par l'égalité exacte, raté par l'ancien `toContain`) ; restauré → vert. Aucune migration BD, aucun changement moteur, aucun changement de payload ni de code de production. Gates mesurés : vitest **224** (+5 ; 147 passés + 77 isolation RLS ignorés faute de Postgres local), tsc 0, biome 0. Branche : `claude/prompt-executer-sprint-526ypa`.

### Sprint 19 — Interrupteur de la Fenêtre de sommeil ✅
**Feature dirigée par l'auteur** (réponse en session ; cadre FR-6, sommeil). **Réconciliation d'abord** (loi anti-hallucination) : le pick naturel des SPRINTS SUGGÉRÉS — « garde de parité d'une copie Edge des échéances » — reposait sur une prémisse **fausse** → STOP. Vérifié en session : aucun miroir TS↔TS non gardé n'existe (`payload.ts` et le format de date sont déjà gardés, Sprints 16/18) ; `lib/notifications/echeances.ts` n'a **pas** de copie Edge — `send-reminders/index.ts:66` lit des `remind_at` **pré-calculés**, la duplication des offsets `30/7/1` est TS↔SQL, déjà gardée par `supabase/tests/reminders.test.ts`. Direction re-choisie par l'auteur : **activer/désactiver la Fenêtre de sommeil**. **Livré** : le travailleur peut éteindre l'affichage de sa fenêtre de sommeil. **Modèle** : colonne `sleep_defaults.enabled` (boolean, défaut `true`) — migration `20260625120000_sprint19_sleep_enabled.sql` ; **disponibilité partagée** (policy `sleep_defaults_all` inchangée — la conjointe la lit, **aucun motif en jeu, R7 intact**) ; heures conservées même désactivé (réactiver = retrouver sa fenêtre). **Moteur pur** : paramètre **en queue** `sleepEnabled = true` sur `dayStatuses`/`statusForDate`/`monthGrid` (`lib/schedule/status.ts`) ; la garde tient en une ligne — `sleeping = sleepEnabled && shift === null && previousShift === "nuit"` ; désactivé, la récupération redevient `conge` (travailleur) / `disponible` (conjointe), `sleep: null`. Défaut `true` ⇒ **zéro régression** (tous les call-sites et tests existants inchangés). **Frontière** : `parseSleepEnabled` (`lib/schedule/db-rows.ts`, parseur frère de `parseSleepRow` — défaut `true` sans ligne). **UI** : case à cocher accessible (libellé + texte, jamais la couleur seule — NFR-12 ; cible ≥ 44 px) dans `components/sommeil/fenetre-sommeil.tsx` ; server action `definirFenetreSommeil` persiste l'état (`active` absent = case décochée = désactivé, sémantique HTML). **Câblage** : `app/page.tsx` lit `enabled` dans le **chemin partagé** (worker ET conjointe), passe `sleepEnabled` à `VueCoupDoeil` (threadé aux 4 appels moteur) ; `/foyer` préremplit la case. **Types** : `enabled` ajouté à `lib/database.types.ts` (hand-edit fidèle au générateur, ordre alphabétique — régénération Cloud par l'auteur, pg_graphql absent du Postgres local). **Preuve d'acceptation observable** (constatée en session, dev SSR HTTP 200) : `/demo/horaire?equipe=C&date=2026-06-05` → pastille **« Sommeil » + plage 07:00–15:00** ; même URL `&sommeilActif=0` → pastille **« Congé », aucune plage** (param démo ajouté). **Revue indépendante** (2 angles contexte frais — correction + R7/conventions/cross-file) : **0 finding** ; passe `/simplify` (2 angles) : 1 cleanup appliqué (ternaire imbriqué aplati dans `fenetre-sommeil.tsx`), reste bien nivelé. Gates mesurés : vitest **232** (+8 : 6 moteur interrupteur + 2 isolation RLS, **exécutés contre un vrai Postgres** via `scripts/local-db.sh` — 35 tests RLS), tsc 0, biome 0, build OK. Branche : `claude/prompt-executer-sprint-klvtk1`.

### Sprint 20 — Note du frigo (tableau partagé, accusés de lecture, push) ✅
**Feature dirigée par l'auteur, hors FR-1→17** (consignée : pas dans `docs/analyse/`). **Réconciliation d'abord** (loi anti-hallucination) : la feature est **réellement neuve** — les « notes » FR-8 (`public.notes`, `components/notes/note-du-jour.tsx`) sont **par date civile**, sans accusé de lecture ; aucun push event-driven ni Realtime n'existait (push **cron-only** via `send-reminders`). **Livré** : nouvelle page `/frigo` — tableau de notes libres où **les deux membres** postent, **accusé de lecture** notifié à l'auteur quand l'autre lit, **seul l'auteur retire**. **Schéma** : table `fridge_notes` (migration `20260626120000_sprint20_fridge_notes.sql`) — accusé plat `read_at`/`read_by`, policies **SPLIT** (SELECT/INSERT/UPDATE membre, **DELETE auteur seul**), INSERT durci `read_* null` (forger un accusé impossible). **Accusé inter-membre** par RPC `marquer_note_frigo_lue` **SECURITY DEFINER** (membre + non-auteur + idempotent ; pas d'UPDATE libre du corps). **Live** : table publiée dans `supabase_realtime` (garde d'existence → migration sûre sur le Postgres local sans la publication) ; composant client `tableau-frigo.tsx` s'abonne aux `postgres_changes` (INSERT/UPDATE/DELETE filtrés foyer), auto-marquage à l'affichage, « Lu ✓ » live. **Push device event-driven** : Edge `notify-fridge` (calque `send-reminders`) déclenchée best-effort par la server action (`fetch`+service-role, timeout 2,5 s, échec jamais bloquant) ; payload pur `fridge-payload.ts` **R7-structurel** (ne reçoit QUE le type d'événement, jamais le corps) + miroir Deno gardé par `fridge-payload-parity.test.ts` (lignée Sprints 16/18). **UI/UX (NFR-12)** : cartes « collées au frigo », icône+texte (jamais couleur seule), cibles ≥44 px, badge « Nouveau », lien d'accueil à pastille. **Revue indépendante** `/code-review` high (3 angles, contexte frais) : RLS/R7 **étanche** confirmé ; corrigés — `.then()`→`async/await`+`useTransition`, parser Realtime réutilise `parseFrigoRows` (frontière Zod unique), accusé optimiste par fusion ciblée (anti-écrasement d'un UPDATE concurrent), anti-tempête de RPC, filtre foyer sur DELETE, INSERT durci, timeout push, couplage env documenté ; passe `/simplify` : `creerNoteFrigo` réutilise `parseFrigoRows`, helper mort retiré. **Preuve d'acceptation observable** (dev SSR, HTTP 200) : `/demo/frigo` (monte le **vrai** `TableauFrigo`, `realtimeActif=false`) montre pose, retrait auteur-seul, badge « Nouveau », et les états « Lu ✓ » / « Pas encore lu ». Gates mesurés : vitest **257** (+25 : 13 isolation RLS **contre un vrai Postgres** via `scripts/local-db.sh` + 6 types + 6 payload/parité), tsc 0, biome 0, build OK. **Tâches opérationnelles** (hors session) : déployer `notify-fridge` + secrets + `SUPABASE_SERVICE_ROLE`/URL functions Vercel ; appliquer la migration sur Cloud + régénérer les types. Branche : `claude/prompt-executer-sprint-hxbfr3`.

### Sprint 21 — Édition d'une note du frigo par son auteur ✅
**Feature dirigée par l'auteur, suite Sprint 20** (complète le CRUD du tableau du frigo : créer/lire/retirer → **modifier**). **Réconciliation d'abord** (loi anti-hallucination) : la policy RLS `fridge_notes_update` (auteur seul) **existe déjà** depuis Sprint 20 (`supabase/migrations/20260626120000_sprint20_fridge_notes.sql:59-61`, préparée « pour l'édition future du corps ») → **aucune migration de schéma nécessaire**. **Décisions produit (auteur, en session)** : (1) l'édition **réinitialise l'accusé de lecture** (le contenu a changé → la note redevient « non lue », `read_at/read_by` → null) ; (2) push **uniquement si la note était déjà lue** (contenu périmé à re-signaler), event `"modifiee"`. **Livré** — server action `modifierNoteFrigo` (`app/frigo/actions.ts`, calquée sur `supprimerNoteFrigo` : `getUser()`, Zod `frigoBodySchema`+`uuidSchema`, double-garde `.eq("author_id")`, lit l'état lu AVANT pour décider du push, UPDATE qui **hardcode `read_at/read_by` à null** — reset voulu ET non-forge d'accusé) ; UI d'édition inline auteur-seul dans `components/frigo/tableau-frigo.tsx` (bouton ✏️ à côté de Retirer, textarea préremplie auto-focus, Enregistrer/Annuler, cibles ≥ 44 px, icône+texte NFR-12) ; nouvel event push `"modifiee"` propagé au miroir TS↔Deno (`lib/notifications/fridge-payload.ts` + `supabase/functions/_shared/fridge-payload.ts`, parité gardée) et à l'Edge `notify-fridge` (validation + destinataire = l'autre membre, comme `"nouvelle"`). **Correctif d'interaction** (trouvé en session) : le handler Realtime UPDATE retire l'id du garde anti-tempête `dejaMarquees` quand `readAt` repasse à null, sinon une note rééditée réafficherait « Nouveau » chez le conjoint sans jamais redevenir « lue » (boucle édition → re-lecture → accusé restaurée). **Durcissement RLS issu de la revue indépendante** : la policy UPDATE Sprint 20 laissait l'auteur **forger** un accusé non-nul sur sa propre ligne (asymétrie avec la policy INSERT déjà durcie) → migration `20260626130000_sprint21_fridge_update_no_receipt_forge.sql` resserre le `with check` (`read_at is null and read_by is null`) ; invariant net : un accusé n'est posé QUE par le RPC `marquer_note_frigo_lue` (SECURITY DEFINER, exempt de RLS), effacé QUE par une édition de l'auteur. **Preuve d'acceptation observable** (Chromium réel sur `/demo/frigo`, 0 erreur JS) : clic ✏️ sur une note à soi déjà lue (« Lu ») → textarea préremplie du corps actuel → Enregistrer → **nouveau corps affiché** + note repassée à **« Pas encore lu »** (reset constaté) ; la note de l'autre membre **n'a aucun bouton ✏️** (auteur seul). **Revue indépendante** `/code-review` high (contexte frais) : 1 MAJOR (forge d'accusé par l'auteur) corrigé par la migration + 1 test ; 3 mineurs appliqués (auto-focus textarea, reset du bandeau d'erreur à l'ouverture, démo conserve `createdAt` pour ne pas réordonner) ; passe `/simplify` : 0 changement (code aligné sur les patterns Sprint 20). Gates mesurés : vitest **265** (+8 : 6 isolation RLS frigo **contre un vrai Postgres** via `scripts/local-db.sh` — auteur édite / non-auteur bloqué / reset d'accusé / non-forge auteur / non-reset non-auteur / CHECK corps vide ; +2 payload `"modifiee"`), tsc 0, biome 0, build OK. **Tâche opérationnelle** (hors session) : appliquer la migration Sprint 21 sur Cloud (cf. tâches en attente) ; redéployer `notify-fridge` (event `"modifiee"`). Branche : `claude/prompt-executer-sprint-2f5v2g`.

### Sprint 22 — Indicateur « Édité » de note du frigo ✅
**Feature dirigée par l'auteur, suite Sprint 21** (distingue une note **rééditée** d'une note neuve). **Réconciliation d'abord (loi anti-hallucination) — prémisse de la carte CORRIGÉE** : la carte proposait « badge si `updatedAt > createdAt` », **sans schéma**, en s'appuyant sur la colonne `updated_at` existante (Sprint 20). Vérifié en session : c'était **faux** — le trigger partagé `set_updated_at` (`supabase/migrations/20260611192620_initial_schema.sql:18`) bumpe `updated_at` à **chaque** UPDATE, et le RPC d'accusé `marquer_note_frigo_lue` (`20260626120000_sprint20_fridge_notes.sql:103-109`) fait un UPDATE de `read_at/read_by` → une note simplement **lue** aurait `updated_at > created_at` et serait faussement « Éditée ». STOP + signalement ; l'auteur a choisi la version **corrigée**. **Livré** : un trigger **propre à `fridge_notes`** ne bumpe `updated_at` **que si `body` change** — `set_fridge_notes_updated` (`set search_path = ''`), migration `20260626140000_sprint22_fridge_edited_trigger.sql` ; le trigger partagé `set_updated_at` reste **intact sur les 9 autres tables** (vérifié). `now()` étant stable en transaction, une note fraîche a `updated_at == created_at` exactement → une lecture ne bouge plus rien, seule une vraie édition avance `updated_at`. **Aucun nouveau champ** (`updated_at` existait déjà, présent dans `lib/database.types.ts`). **Threading** : `updatedAt` ajouté à `FrigoNote` (`lib/frigo/types.ts`), à `parseFrigoRows` + son schéma Zod (`lib/frigo/db-rows.ts`) et aux **3 SELECT** de notes complètes (`app/frigo/page.tsx`, `app/frigo/actions.ts` ×2) ; le handler Realtime réutilise `parseFrigoRows`, la colonne arrive donc automatiquement. **Helper pur** `estEditee(note)` (`lib/frigo/types.ts`) : comparaison en **temps epoch** (`getTime()`, robuste aux offsets de fuseau), jamais lexicographique. **UI** : badge discret « ✎ Édité » (icône+texte, NFR-12) dans le pied de carte, visible des **deux** membres ; i18n `fr.frigo.edite`. **R7 intact** : `updated_at` est un horodatage non sensible ; aucun motif/paye/corps de note ne fuit. **Preuve d'acceptation observable** (Chromium réel sur `/demo/frigo`, 0 erreur JS hors 404 d'asset bénin) : la note éditée (`updatedAt > createdAt`) affiche **✎ Édité** ; la note **lue mais non éditée** (« ✓ Lu ») n'affiche **PAS** « Édité » (preuve : lire ≠ éditer) ; les notes fraîches sans badge. **Revue indépendante** `/code-review` high (3 angles finder, contexte frais) : **0 finding** (shared trigger non modifié, `search_path` sûr, INSERT donne `created==updated`, payload Realtime complet, types déjà présents, conventions OK) ; passe `/simplify` : code déjà propre. Gates mesurés : vitest **272** (+7 : 3 `estEditee` + 2 `parseFrigoRows` + 2 isolation RLS — lecture ne bumpe pas `updated_at` / édition l'avance — **contre un vrai Postgres** via `scripts/local-db.sh`), tsc 0, biome 0, build OK. **Migration appliquée sur Cloud** (post-fusion, via Supabase MCP) : trigger `set_fridge_notes_updated` actif en prod, lecture ne pose plus « Édité » (vérifié) ; au passage, la RLS owner-only de `cycle_templates` (**Sprint 14, manquante sur Cloud**) a été appliquée. Branche : `claude/prompt-executer-sprint-ngjtyp` (PR #50 → `dev`, release #51 → `main`).

### Sprint 23 — Réponses au frigo (fil à un seul niveau) ✅
**Feature dirigée par l'auteur, suite Sprints 20-22** (complète le tableau du frigo vers un canal léger : un membre peut **répondre** à une note — **un seul niveau**, une réponse ne se répond pas). **Réconciliation d'abord** (loi anti-hallucination) : prémisse **propre** — `fridge_notes` n'avait **aucune** colonne `parent_id` (`supabase/migrations/20260626120000_sprint20_fridge_notes.sql:14-25`), `FrigoNote`/`parseFrigoRows` vérifiés (`lib/frigo/types.ts:10-18`, `lib/frigo/db-rows.ts:8-32`). **Décisions produit (auteur, en session)** : (D1) **pas d'accusé de lecture sur les réponses** (une réponse EST l'accusé) ; (D2) **push activé**, nouvel event `"reponse"`, destinataire = l'autre membre ; (D3) badges minimaux sur réponse (seul « ✎ Édité »). **Schéma** : colonne `parent_id` auto-référente nullable (`on delete cascade`) — migration `20260627120000_sprint23_fridge_note_replies.sql` ; trigger `enforce_fridge_note_single_level` (`search_path=''`) garde l'invariant **au niveau données** : parent doit être une note de tête, même foyer, et une note **ayant des réponses ne peut pas devenir une réponse** (bloque le re-parentage qui créerait Q→P→C, durcissement issu de la revue) ; index partiel `idx_fridge_notes_parent` ; RPC `marquer_note_frigo_lue` resserré (`and f.parent_id is null` → refuse un accusé sur une réponse, D1). **RLS inchangée** : les 4 policies portent déjà sur `household_id`+`author_id`, valables pour une ligne de réponse ; le `with check` durci (Sprint 21, `read_*` nuls) garantit déjà une réponse sans accusé. **Helper pur** `grouperEnFils` (`lib/frigo/types.ts`) regroupe la liste plate en fils (têtes récentes d'abord, réponses chronologiques, orphelines écartées) ; la règle D1 vit dans `statutLecture`/`estNouvellePourMoi` (réponse → « non-applicable »/false) pour que **tout** consommateur (badge d'accueil, auto-marquage) la respecte sans double garde. **Threading** : `parentId` ajouté à `FrigoNote`, `parseFrigoRows`+schéma Zod, et aux SELECT via la constante partagée `FRIGO_NOTE_COLUMNS` (un seul endroit à toucher). **Actions** : `repondreNoteFrigo` (`app/frigo/actions.ts`) dérive `household_id` de la note parente lue sous RLS (zéro confiance client), insère `parent_id`, push `"reponse"`. **UI** (`components/frigo/tableau-frigo.tsx`) : fils mémoïsés, réponses indentées sous leur note de tête, composeur de réponse inline (focus au montage), « Répondre » sur les notes de tête seulement (ouvert aux deux membres) ; `CarteNote` dérive « est une réponse » de `note.parentId`. **Push** : event `"reponse"` propagé au miroir TS↔Deno (`lib/notifications/fridge-payload.ts` + `supabase/functions/_shared/fridge-payload.ts`, parité gardée) + validation Edge `notify-fridge` ; **R7** : le payload ne porte que le type d'événement, jamais le corps. **Correctif de revue** : la pastille « non lues » de l'accueil (`app/page.tsx`) filtrait sans `parent_id is null` — une réponse (jamais marquable, D1) figeait le compteur → corrigé. **Preuve d'acceptation observable** (Chromium réel sur `/demo/frigo`, 0 erreur JS) : 4 notes de tête montrent « Répondre » ; la réponse semée s'affiche **indentée** dans `ul[aria-label="réponses"]` et **n'a aucun** « Répondre » (un seul niveau) ; le composeur poste une **nouvelle réponse indentée** sous son parent. **Revue indépendante** `/code-review` high (8 angles, contexte frais) : 1 bug confirmé corrigé (pastille d'accueil) + altitude (règle D1 poussée dans les helpers) + durcissement trigger (re-parentage) + exclusivité des modes édition/réponse ; passe `/simplify` (4 angles) : `FRIGO_NOTE_COLUMNS` partagé, `estReponse` dérivé de `note.parentId` (prop retirée), `grouperEnFils` mémoïsé. Gates mesurés : vitest **288** (+16 : 4 `grouperEnFils` + 5 `parseFrigoRows`/parentId + 7 isolation RLS frigo — réponse même foyer / cross-foyer refusée / un seul niveau (insert + re-parentage UPDATE) / mismatch foyer / cascade / RPC refuse une réponse — **contre un vrai Postgres** via `scripts/local-db.sh`), tsc 0, biome 0, build OK. **Tâche opérationnelle** (hors session) : appliquer la migration Sprint 23 sur Cloud + régénérer les types ; redéployer `notify-fridge` (event `"reponse"`). Branche : `claude/prompt-executer-sprint-gyj97n`.

### Sprint 24 — Épingler une note du frigo (une seule par foyer, auteur seul) ✅
**Feature dirigée par l'auteur, suite Sprints 20-23** (prolonge le tableau du frigo : une note de tête peut rester **épinglée en haut**, cas d'usage liste d'épicerie permanente). **Décisions produit (auteur, en session)** : portée **auteur seul** (on n'épingle/détache que sa propre note) ; **une seule épingle par foyer** (épingler en détache la précédente) ; **pas de push** (action de tri, faible signal). **Réconciliation d'abord — prémisse de la carte CORRIGÉE** : la carte proposait « simple colonne + petite UPDATE, **pas de RPC** ». Vérifié en session : c'était **faux** — la policy `fridge_notes_update` durcie au Sprint 21 (`20260626130000_sprint21_fridge_update_no_receipt_forge.sql:18-23`) impose `read_at/read_by` **nuls** dans son `with check` → un `UPDATE … set is_pinned` direct sur une note **déjà lue** échouerait (ou forcerait un reset d'accusé erroné) ; et « une épingle par foyer » exige de détacher l'épingle de l'**autre** membre, interdit par la RLS auteur-seul. **Livré** : migration `20260627130000_sprint24_fridge_note_pin.sql` — colonne `is_pinned` (défaut false), CHECK `fridge_notes_pin_head_only` (`not (is_pinned and parent_id is not null)` → une réponse n'est jamais épinglée), **index unique partiel** `idx_fridge_notes_one_pin (household_id) where is_pinned` (rempart BD tous chemins), et RPC **`epingler_note_frigo(note_id, pin)` SECURITY DEFINER** (calque `marquer_note_frigo_lue`) : vérifie **membre du foyer** (parité — un membre **révoqué** garde author_id, sans cette garde il pourrait encore épingler par appel RPC direct), **auteur seul**, **tête seulement**, puis détache l'épingle existante du foyer avant de poser la nouvelle. Le RPC ne touche **ni `body` ni `read_at`** → le trigger Sprint 22 ne bumpe pas `updated_at` (pas de faux « Édité ») et l'accusé n'est jamais réinitialisé. **Threading** : `is_pinned`/`isPinned` ajouté via la source unique `FRIGO_NOTE_COLUMNS` + schéma Zod + `parseFrigoRows` (`lib/frigo/db-rows.ts`), `FrigoNote.isPinned` (`lib/frigo/types.ts`), `lib/database.types.ts` (hand-edit fidèle + signature RPC). **Tri à deux niveaux** dans `grouperEnFils` : épingle d'abord (`Number(b.isPinned) - Number(a.isPinned)`), puis récent-d'abord. **Action** `epinglerNoteFrigo` (`app/frigo/actions.ts`, RPC ; pas de push) ; **UI** (`components/frigo/tableau-frigo.tsx`) : bouton 📌 Épingler/Détacher auteur-seul sur les notes de tête + badge « 📌 Épinglée » visible des **deux** membres, mise à jour optimiste (une épingle/foyer reflétée localement) ; i18n `fr.frigo.{epingler,detacher,epinglee,erreurEpinglage}`. **R7 intact** : `is_pinned` est un drapeau non sensible. **Preuve d'acceptation observable** (Chromium réel sur `/demo/frigo`, **0 erreur JS**) : la note de l'**autre** membre pré-épinglée (seed) montre le badge mais **aucun** bouton bascule (auteur seul) ; épingler **ma** note → elle **remonte en tête** avec le badge ET l'épingle précédente est **détachée** (une seule épingle, cross-auteur) ; **aucune réponse** n'a de bouton d'épingle (tête seulement). **Revue indépendante** `/code-review` high (8 angles, contexte frais) : 1 finding **confirmé** corrigé — le RPC omettait la garde `is_household_member` du RPC frère (un membre **révoqué** aurait pu épingler ses anciennes notes par appel direct) → garde ajoutée + test ; reste = refus vérifiés (Realtime livre toujours la ligne courante complète → pas de réversion d'épingle ; bouton aligné sur ses pairs non-`disabled`) ; passe `/simplify` : 0 changement (code aligné Sprints 20-23). Gates mesurés : vitest **300** (+12 : 7 isolation RLS frigo **contre un vrai Postgres** via `scripts/local-db.sh` — auteur épingle / non-auteur refusé / une épingle cross-auteur / réponse refusée / index unique / non-bump updated_at + accusé préservé / **membre révoqué refusé** ; +2 `grouperEnFils` épingle-d'abord ; +3 `parseFrigoRows` is_pinned), tsc 0, biome 0, build OK. **Tâche opérationnelle** (hors session) : appliquer la migration Sprint 24 sur Cloud + régénérer les types. Branche : `claude/prompt-executer-sprint-xyu9wu`.

### Sprint 25 — Listes d'épicerie dans le frigo (partagées, cochage à deux, push anti-spam) ✅
**Feature dirigée par l'auteur** (définie en session ; hors FR-1→17, consignée). Prolonge l'espace du frigo (Sprints 20-24) vers un nouvel usage : des **listes d'épicerie** où **les deux membres** créent une liste, ajoutent/retirent des articles et **cochent/décochent** ce qui est acheté. **Réconciliation d'abord** (loi anti-hallucination) : tout le code épicerie était **à créer** (aucune table/type/action `epicerie` n'existait — vérifié) ; le helper RLS `is_household_member` (`supabase/migrations/20260611192623_rls_policies.sql:14`) et l'infra push (`push_subscriptions`, VAPID, Resend, gabarit `notify-fridge`) **existaient** et ont été réutilisés (aucun nouveau secret/env). **Décisions produit (auteur, en session)** : (1) **gestion à deux** (ajouter/retirer/cocher ouverts aux deux membres) ; (2) **placement** = section dédiée « Épicerie » dans `/frigo`, séparée du tableau de notes ; (3) **push au cochage anti-spam** (le 1ᵉʳ cochage d'une liste notifie le conjoint, puis silence pendant une fenêtre de cooldown). **Schéma** : 2 tables (migration `20260628120000_sprint25_grocery_lists.sql`) — `grocery_lists` (titre, `last_check_notified_at` = débounce **interne**, jamais sélectionné/forgeable côté client) et `grocery_items` (`is_checked`/`checked_by`/`checked_at`, `household_id` dénormalisé pour RLS+Realtime, CHECK de cohérence). **RLS partagée** : SELECT/INSERT/DELETE = membre du foyer ; **aucun grant/policy UPDATE** → l'état de coche ne bascule **que** par le RPC `cocher_element_epicerie` (SECURITY DEFINER, `search_path=''`, calque `marquer_note_frigo_lue`) qui vérifie `is_household_member` (**membre révoqué refusé**), est idempotent, et tient le débounce ; INSERT durci (coche non forgeable) ; trigger `enforce_grocery_item_household` (anti-injection cross-foyer, INSERT+UPDATE). **Realtime** : les 2 tables publiées (garde d'existence comme `fridge_notes`). **Couche pure** : `lib/epicerie/{types,db-rows}.ts` — `GROCERY_LIST_COLUMNS`/`GROCERY_ITEM_COLUMNS` (source unique), frontière Zod `parseGrocery*Rows`, helpers `grouperListes`/`compteRestant`. **Actions** `app/epicerie/actions.ts` (`getUser()` + RLS + `revalidatePath`) ; push best-effort via `notify-grocery-client.ts` → Edge `notify-grocery` (calque `notify-fridge` ; destinataire = l'autre membre ≠ acteur). **R7** : payload `grocery-payload.ts` + copie Deno (parité testée) ne portent **que** le type d'événement, jamais un libellé d'article. **UI** `components/epicerie/listes-epicerie.tsx` (cases ☑/☐ ≥ 44 px icône+texte, libellé barré + « coché par X », badge « X à acheter », mise à jour optimiste, Realtime), intégrée à `app/frigo/page.tsx` ; i18n `fr.epicerie`. **Preuve d'acceptation observable** (Chromium réel sur `/demo/epicerie`, **0 erreur JS applicative** — seul un 404 favicon bénin, confirmé par curl) : liste seedée « 2 à acheter », article « Pain » coché « par Conjoint(e) » ; cocher « Lait » → « 1 à acheter », décocher → « 2 à acheter » ; création d'une 2ᵉ liste + ajout d'article. **Revue indépendante** `/code-review` high (8 angles, contexte frais) : **0 bug de correction / 0 faille RLS / 0 violation CLAUDE.md** confirmés (candidats réfutés : `upsertItem` n'affecte pas l'ordre car `grouperListes` re-trie ; `revalidatePath("/")` inutile, pas d'indicateur épicerie sur l'accueil) ; passe `/simplify` : 1 import mort `useRef` retiré (duplications `functionsBaseUrl`/garde d'auth gardées — parité voulue avec le frigo). Gates mesurés : vitest **336** (+36 : 18 isolation RLS épicerie **contre un vrai Postgres** via `scripts/local-db.sh` — isolation foyer / les deux cochent / révoqué refusé / coche non forgeable / UPDATE direct refusé / débounce / cascade ; +8 helpers purs ; +6 frontière Zod ; +3 payload ; +1 parité), tsc 0, biome 0, build OK. **Tâches opérationnelles** (hors session) : appliquer la migration Sprint 25 sur Cloud + régénérer les types ; **déployer l'Edge `notify-grocery`** (+ vérifier `SUPABASE_SERVICE_ROLE`/URL functions). Branche : `claude/prompt-executer-sprint-494ccj`.
